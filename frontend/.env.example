# Copy to .env and edit. Base URL of the TRACE FastAPI backend.
# Defaults to http://localhost:8000 if unset.
VITE_API_BASE_URL=http://localhost:8000

# To point a local frontend at the deployed backend instead:
# VITE_API_BASE_URL=https://trace-backend-4uu2.onrender.com
